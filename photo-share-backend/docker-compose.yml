services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GOOGLE_DRIVE_FOLDER_ID=${GOOGLE_DRIVE_FOLDER_ID}
      - PORT=3000

  frontend:
    build:
      context: ../photo-share-frontend
    ports:
      - "5173:5173"
